Behaviour Gravity
    Property drag = 9.806

    Update
        movement = Get Behaviour Movement
        If movement != None Then
            movement.velocity.y = movement.velocity.y + (drag * Screen.frameSpeed / 10.0)
        End
    End
End
