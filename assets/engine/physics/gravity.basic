Behaviour Gravity
    Property velocity = 0
    Property drag = 9.806

    Update
        y = y + (velocity * Screen.frameSpeed)
        velocity = velocity + (drag * Screen.frameSpeed / 10.0)
    End
End
